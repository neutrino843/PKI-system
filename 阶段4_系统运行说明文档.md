# PKI演示系统 - 小白也能看懂的使用说明书

> 本文档用最通俗的语言，教你从零开始运行PKI演示系统。
> 即使完全不懂IT技术，也能按步骤完成所有功能演示。

---

## 第一章：环境依赖说明（装软件）

### 1.1 你需要提前安装的软件

运行这套系统，你需要在电脑上装 **2样东西**。就像玩电脑游戏之前要先装运行库一样。

| 序号 | 软件名称 | 版本要求 | 干啥用的 | 下载地址 |
|:----:|---------|---------|---------|---------|
| ① | **Python** | 3.8及以上版本（推荐3.13） | 这是一门编程语言的"运行环境"，我们的PKI系统就是用Python写的，装了它才能运行 | https://www.python.org/downloads/ |
| ② | **cryptography库** | 41.0及以上版本 | 这是一个加密工具箱，装好Python后，用一条命令就能安装，系统用它来生成证书和密钥 | 见下方安装步骤 |

### 1.2 Python安装步骤（一看就会）

#### Windows系统：

1. 打开浏览器，访问 https://www.python.org/downloads/
2. 点击大大的黄色按钮 **Download Python 3.x.x**（最新版本）
3. 下载完成后，双击安装包
4. **重要！** 安装时一定要勾选 [OK] **"Add Python to PATH"**（把Python加入系统路径）
5. 然后点击 **"Install Now"**（立即安装）
6. 等进度条走完，显示"Setup was successful"就装好了

#### 验证是否安装成功：

1. 按键盘上的 `Win + R` 键
2. 输入 `cmd` 然后回车，打开黑色命令窗口
3. 输入以下命令然后回车：
   ```
   python --version
   ```
4. 如果看到类似 `Python 3.13.x` 的字样，说明安装成功了！

### 1.3 安装加密库（一条命令搞定）

1. 打开命令窗口（和上面一样，Win+R → cmd → 回车）
2. 输入以下命令然后回车：
   ```
   pip install cryptography
   ```
3. 等待进度条跑完，显示"Successfully installed cryptography"就装好了

#### 验证是否安装成功：

在命令窗口输入以下命令然后回车：
```
python -c "import cryptography; print(cryptography.__version__)"
```
如果显示出数字（如 46.0.5），说明安装成功！

---

## 第二章：部署步骤指南（启动系统）

### 2.1 找到系统文件

系统文件放在 `pki_demo` 文件夹中，里面有以下内容：

```
pki_demo/
├── main.py                 [0x2605] 主程序（双击运行这个）
├── unit1_root_ca.py        # 独立单元1
├── unit23_key_csr.py       # 独立单元2+3
├── unit4_sign_cert.py      # 独立单元4
├── unit5_pkcs12.py         # 独立单元5
├── unit67_crl.py           # 独立单元6+7
├── certs/                  # 存放证书的文件夹
├── keys/                   # 存放密钥的文件夹
├── csr/                    # 存放申请的文件夹
├── crl/                    # 存放吊销列表的文件夹
├── export/                 # 存放导出文件的文件夹
└── requirements.txt        # 依赖清单（不用管它）
```

### 2.2 启动系统步骤

#### 第一步：打开命令窗口并进入系统目录

1. 打开 `pki_demo` 文件夹
2. 在文件夹的地址栏（顶部的白色长条）里输入 `cmd` 然后回车
3. 会弹出一个黑色命令窗口，里面的路径要显示为 `...\pki_demo>`

#### 第二步：启动主程序

在黑色命令窗口中输入以下命令，然后回车：

```
python main.py
```

#### 第三步：成功启动的标志

你会看到系统中出现一个菜单界面，类似这样：

```
  ╔══════════════════════════════════════════╗
  ║     PKI演示系统 - 电子身份证管理平台      ║
  ╚══════════════════════════════════════════╝

  [A] 根CA管理      - 管理发证总局
  [B] 用户证书管理   - 申请和签发电子身份证
  [C] 证书吊销管理   - 挂失、发布CRL、查状态
  [Q] 退出系统
```

到这里，系统就启动成功了！接下来就可以进行功能演示了。

---

## 第三章：核心功能操作演示流程

### 场景设定（讲故事）

小明是一家公司的IT管理员，今天他要用这套PKI系统：
1. 先建立公司自己的"发证总局"（根CA）
2. 为员工张三和李四办理"电子身份证"（数字证书）
3. 张三离职了，吊销他的身份证
4. 查询李四的身份证是否仍然有效

---

### 【操作1】创建根CA（成立"发证总局"）

**这一步在做什么？**
> 就像国家要成立身份证管理局，先要把管理局的"公章"（私钥）和"成立证书"（自签证书）准备好。

**操作步骤：**

| 步骤 | 操作 | 屏幕显示 | 通俗解释 |
|:----:|------|---------|---------|
| ① | 在主菜单输入 `A` 然后回车 | 进入根CA管理模块 | 选择了"发证总局管理"功能 |
| ② | 在子菜单输入 `1` 然后回车 | 进入创建根CA页面 | 选了"创建发证总局" |
| ③ | 根CA名称直接回车（默认：演示根CA） | 显示"请输入根CA名称" | 给发证总局起个名字 |
| ④ | 组织名直接回车（默认：PKI演示系统） | 显示"请输入组织名称" | 写公司名称 |
| ⑤ | 有效期直接回车（默认：10年） | 显示"请输入证书有效期" | 设置总局证书多久过期 |
| ⑥ | 等待几秒钟 | 显示"正在生成根CA..." | 电脑在算密码，稍等片刻 |
| ⑦ | 显示成功信息 | 显示根CA创建成功 | 发证总局成立完毕！ |

**成功标志：**
```
  [OK] 根CA创建成功！
  [0x1f4c1] 私钥文件：keys/root_ca_private.pem
  [0x1f4c1] 证书文件：certs/root_ca_cert.pem
```

---

### 【操作2】申请新证书（员工办身份证）

**这一步在做什么？**
> 员工张三想领一张电子身份证，他先自己配好"锁和钥匙"（密钥对），然后填写"申请表"（CSR），提交给发证机关。

**操作步骤：**

| 步骤 | 操作 | 通俗解释 |
|:----:|------|---------|
| ① | 回到主菜单（按回车后输入 `B` 再回车） | 进入"办证中心" |
| ② | 输入 `1` 然后回车 | 选择"申请新证书" |
| ③ | 用户名输入 `张三` 然后回车 | 填写申请人姓名 |
| ④ | 部门输入 `研发部` 然后回车 | 填写所在部门 |
| ⑤ | 等待几秒钟 | 电脑在生成密钥和申请表 |
| ⑥ | 显示成功信息 | 申请提交成功！ |

**重复以上步骤，把"李四"的申请也提交了。**

**成功标志：**
```
  [OK] 申请完成！
  [0x1f4c1] 私钥：keys/user_张三_private.pem（加密保存）
  [0x1f4c1] CSR：csr/user_张三_csr.pem（待CA审核）
```

---

### 【操作3】CA签发证书（发证机关审核发证）

**这一步在做什么？**
> 发证机关（CA）审核张三的申请表，确认没问题后，在申请表上"盖钢印"（签名），正式的制作出一张电子身份证（数字证书）。

**操作步骤：**

| 步骤 | 操作 | 通俗解释 |
|:----:|------|---------|
| ① | 在办证中心子菜单输入 `2` 然后回车 | 选择"CA签发证书" |
| ② | 系统自动处理所有待办申请 | CA自动审核所有提交的申请表 |
| ③ | 显示签发成功 | 身份证制作完成！ |

**成功标志：**
```
  [OK] 签发完成！共处理 2 个证书。
  [0x1f4c1] 证书文件：certs/user_张三_cert.pem
  [0x1f4c1] 证书文件：certs/user_李四_cert.pem
```

---

### 【操作4】查看已签发的证书

**这一步在做什么？**
> 检查一下已经签发了哪些员工的电子身份证。

**操作步骤：**

| 步骤 | 操作 | 通俗解释 |
|:----:|------|---------|
| ① | 在办证中心子菜单输入 `3` 然后回车 | 选择"查看已签发的证书" |
| ② | 系统列出所有证书 | 看到张三和李四的证书信息 |
| ③ | 按回车键返回 | 看完后继续 |

---

### 【操作5】吊销证书（挂失身份证）

**这一步在做什么？**
> 张三离职了，需要把他的电子身份证"挂失"作废，以防他离职后还用公司证件。

**操作步骤：**

| 步骤 | 操作 | 通俗解释 |
|:----:|------|---------|
| ① | 回到主菜单，输入 `C` 回车 | 进入"挂失中心" |
| ② | 输入 `1` 然后回车 | 选择"吊销证书" |
| ③ | 输入 `1` 选择张三 | 选择要吊销谁的证 |
| ④ | 输入 `1` 选择"隶属关系变更" | 选择吊销原因（离职） |
| ⑤ | 显示吊销成功 | 挂失完成！ |

**成功标志：**
```
  [OK] 证书 '张三' 已成功吊销！
  原因：隶属关系变更（如离职）
```

---

### 【操作6】生成CRL（发布"挂失名单"）

**这一步在做什么？**
> 挂失完要发一个正式的"挂失名单公告"（CRL），让所有人都知道张三的身份证已经作废了。这份公告由发证机关签名确认，防止造假。

**操作步骤：**

| 步骤 | 操作 | 通俗解释 |
|:----:|------|---------|
| ① | 在挂失中心输入 `2` 然后回车 | 选择"生成CRL" |
| ② | 系统自动生成CRL文件 | 发证机关发布挂失名单公告 |
| ③ | 显示生成成功 | 公告已发布！ |

**成功标志：**
```
  [OK] CRL生成成功！
  [0x1f4c1] 文件：crl/ca_crl.pem
```

---

### 【操作7】查询证书状态（查身份证是否有效）

**这一步在做什么？**
> 查一下张三的身份证是不是真的挂失了，李四的是不是还能正常使用。

**操作步骤：**

| 步骤 | 操作 | 通俗解释 |
|:----:|------|---------|
| ① | 在挂失中心输入 `3` 然后回车 | 选择"查询证书状态" |
| ② | 文件路径直接回车（默认查张三） | 查张三的证书 |
| ③ | 系统显示：已吊销 [FAIL] | 张三的证确实已挂失！ |
| ④ | 重复①，输入李四的证书路径 | 查李四的证书 |
| ⑤ | 系统显示：有效 [OK] | 李四的证还是好的！ |

**成功标志：**
```
  [0x1f50d] 张三：状态 → 已吊销 [FAIL]
  [0x1f50d] 李四：状态 → 有效 [OK]
```

---

### 【操作8】导出PKCS#12个人证书

**这一步在做什么？**
> 把员工的电子身份证打包成一个文件（.p12格式），这样员工可以把证书导入到浏览器或系统中使用。就像把身份证做成电子版存在手机里。

**操作步骤：**

| 步骤 | 操作 | 通俗解释 |
|:----:|------|---------|
| ① | 在挂失中心输入 `5` 然后回车 | 选择"导出PKCS#12" |
| ② | 输入 `2` 选择李四 | 选一个有效的证书导出 |
| ③ | 设置导出密码（直接回车用默认密码） | 给证书保险箱设密码 |
| ④ | 导出成功 | 个人证书打包完成！ |

**成功标志：**
```
  [OK] PKCS#12导出成功！
  [0x1f4c1] 文件：export/user_李四.p12
  [0x1f511] 密码：p12_123
```

---

## 第四章：演示效果验证方法

### 验证清单

做完以上所有操作后，用下面这些问题来检查系统是否正常工作：

| 编号 | 检查项 | 预期结果 | 验证方法 | 通俗解释 |
|:----:|-------|---------|---------|---------|
| ① | 根CA是否创建成功 | `certs/root_ca_cert.pem` 文件存在 | 查看 certs 文件夹 | 发证总局的证书生成了吗？ |
| ② | 根CA证书是否有效 | 证书信息完整可读 | 在主菜单A→2查看 | 证书信息能正常读取吗？ |
| ③ | 用户证书是否签发成功 | `certs/user_张三_cert.pem` 等文件存在 | 查看 certs 文件夹 | 员工的身份证制作好了吗？ |
| ④ | 证书信任链是否完整 | 用户证书由根CA签发 | 查看证书的颁发者信息 | 员工证上的发证机关是总局吗？ |
| ⑤ | CRL是否生成 | `crl/ca_crl.pem` 文件存在 | 查看 crl 文件夹 | 挂失名单发布了吗？ |
| ⑥ | 吊销功能是否正常 | 张三证书显示"已吊销" | 在挂失中心→3查询 | 张三的证挂失成功了吗？ |
| ⑦ | 有效证书查询是否正常 | 李四证书显示"有效" | 在挂失中心→3查询 | 李四的证还是好的吗？ |
| ⑧ | PKCS#12导出是否正常 | `export/user_李四.p12` 文件存在 | 查看 export 文件夹 | 个人证书能打包导出吗？ |
| ⑨ | CRL签名是否有效 | CRL加载不报错 | 查看CRL生成时的提示 | 挂失名单是总局发布的吗？ |

### 常见问题解决

| 问题 | 原因 | 解决方法 |
|------|------|---------|
| 运行 `python main.py` 报错"不是内部或外部命令" | Python没装好或没加入PATH | 重新安装Python，记得勾选"Add Python to PATH" |
| 运行时报错 `ModuleNotFoundError: cryptography` | 没装加密库 | 运行 `pip install cryptography` |
| 中文显示乱码 | Windows编码问题 | 不影响功能，证书文件内容是正确的 |
| 提示"根CA尚未创建" | 还没初始化 | 先去主菜单A→1创建根CA |

---

## 附录：文件说明

| 文件/文件夹 | 用途 | 通俗解释 |
|------------|------|---------|
| `main.py` | 主程序，启动系统运行这个 | 系统的"开关" |
| `certs/root_ca_cert.pem` | 根CA证书 | 发证总局的"成立证书" |
| `keys/root_ca_private.pem` | 根CA私钥（加密保存） | 发证总局的"公章" |
| `certs/user_xxx_cert.pem` | 用户证书 | 员工的"电子身份证" |
| `keys/user_xxx_private.pem` | 用户私钥（加密保存） | 员工的"钥匙" |
| `csr/user_xxx_csr.pem` | 证书签名请求 | 员工的"身份证申请表" |
| `crl/ca_crl.pem` | 证书吊销列表 | "挂失身份证名单" |
| `export/user_xxx.p12` | 个人证书导出包 | "电子身份证保险箱" |

---

> 恭喜你！你已经完成了整套PKI系统的全流程演示！
> 通过这次演示，你亲手搭建了一个迷你版的"电子身份证管理体系"，
> 体验了从建立发证机关、办理身份证、到挂失作废的完整流程。
